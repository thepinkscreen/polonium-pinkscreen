# Polonium: only the fork-added `scale` command loc lives here. The rest of this file used to
# duplicate RobustToolbox's own en-US commands.ftl, which the engine now rejects as duplicate
# loc entries on load — the engine already provides those keys.
cmd-scale-desc = Zwiększa lub zmniejsza rozmiar encji w prosty sposób.
cmd-scale-help = scale <entityUid> <float>
